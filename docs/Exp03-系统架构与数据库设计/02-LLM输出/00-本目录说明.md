# 本目录说明：实验 3 的 LLM 输出

> 生成工具：Claude Code（Claude Fable 5.1），2026-09-14 晚，操作人孟甲。输入是 `01-手工原稿/ER图-Chen氏-手绘录入版.md` 的实体表、联系表与枚举表，以及实验 2 最终版验收标准中与数据有关的条目。输出只作初稿，人工优化版在 `04-最终版/`，逐条改动理由在 `03-对比与修改记录/SQL优化对比记录.md`。

## 课程要求的顺序

手动绘制类图与 ER 图 → LLM 根据 ER 图生成 SQL 初稿 → 人工优化并保留对比 → 提交设计稿。本目录是第二步的产物，第一步已在 01 目录完成并录入。

## 文件清单

| 文件                    | 内容                                      | 人工需要做什么                                      |
| ----------------------- | ----------------------------------------- | --------------------------------------------------- |
| 01-SQL初稿-LLM版.sql    | MySQL 建表脚本初稿，一次性覆盖两个 Sprint | 周浩然、杨子航逐表批注优化；袁飞翔在 MySQL 8.4 跑通 |
| 02-SQL初稿说明-LLM版.md | LLM 对自己脚本的假设与说明                | 核对假设是否与 ER 图一致，不一致处记入对比记录      |

## 提示词原文

```
你是 MySQL 8 数据库设计师。下面是一个"课程作业 AI 使用备案与过程证据链平台"的 ER 图录入版（Chen 氏，含实体表、联系表、枚举表）。请据此生成完整的 MySQL 建表脚本：
1. 每个实体一张表，M:N 联系落成中间表，弱实体用复合主键或带外键的代理键。
2. 字段类型、长度、非空、默认值、外键约束都要写。
3. 加上你认为必要的索引。
4. 用中文注释说明每张表和关键字段。
5. 一次性给出全部表，包括标注 S2 的实体。
不要省略任何实体。
```

## 已知问题

初稿是按提示词"一次性给出全部"生成的，没有区分 Sprint；对"只追加不更新""增量存储""规则版本化"这些 ER 图批注里的要求理解不到位。这些正是人工优化要处理的，见对比记录。
