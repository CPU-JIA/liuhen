# 走查记录（实验 6）

> 指导书要求"开展代码审查和走查"。审查是各人对着打印稿批注（见代码审查意见），走查是 2026-09-19 晚线上集体过测试代码与本周修改，六人到齐。走的是测试，不是产品代码：产品代码实验 5 走过了。记录：张家森。只评代码不评人。

## 走查清单（每条测试问三句）

1. 断言的是真行为，还是 mock 的返回值原样吐回来？
2. 输入是不是边界？数字从哪来？
3. 这条失败会说明什么？说明不了什么的用例删掉。

## 第一场：袁飞翔讲后端 Service 层测试（SnapshotService、WorkService、Registration、Declaration、Timeline）

| 序号 | 提出人 | 问题                                                                            | 结论                                                                                                 | 谁改   |
| ---- | ------ | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------ |
| 1    | 周浩然 | SnapshotServiceTest 的仓库是内存 List 加 Answer，算 mock 还是假实现             | 是假实现（fake），save 真的存、findById 真的找，所以 21 版关键帧那条断言的是真行为。不改             | 无     |
| 2    | 杨子航 | keyframeAtFirstEleventhAndTwentyFirst 只断言了 1、11、21，中间的呢              | 循环里每一版都断言了 expectKeyframe，是全表。看了代码确认                                            | 无     |
| 3    | 丁浩然 | WorkServiceTest 的 5 万 emoji 那条跑多久                                        | 97 毫秒，emoji 串 10 万 UTF-16 单元，codePointCount 是线性的。不改                                   | 无     |
| 4    | 孟甲   | DeclarationServiceTest 的判定表八格，用例只有五条                               | 表里"任意"合并了格子：B 有时 C 任意是一条，A 有时 B 任意各一条。五条盖八格。不改                     | 无     |
| 5    | 张家森 | 声明用例断言中文标签"修改后采用"，标签改了测试就挂                              | 就是要挂。声明是给学生看的，标签改了本来就该有人知道                                                 | 无     |
| 6    | 周浩然 | TimelineAssemblerTest 的两端一致只比了同一份数据                                | 权限桩是空的，学生与教师走同一方法本来就一样。这条测的是"没有按角色分支"，配合权限测试才完整。加注释 | 袁飞翔 |
| 7    | 杨子航 | RegistrationServiceTest 分隔符回归用例，两次 create 之间 store.clear() 是为什么 | 让两条都从 GENESIS 起链，只比 payload 的差别。加注释                                                 | 袁飞翔 |

## 第二场：袁飞翔讲账号、规则、权限、令牌测试

| 序号 | 提出人 | 问题                                                                                 | 结论                                                                                | 谁改 |
| ---- | ------ | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ---- |
| 8    | 丁浩然 | 假 PasswordEncoder 是 "enc:" + 明文，测试里能看到明文                                | 只在测试；集成测试用真 bcrypt。不改                                                 | 无   |
| 9    | 周浩然 | AccessControlTest.matrixMatchesDesignDocument 的期望表是从代码抄的还是从设计说明抄的 | 从设计说明第 6 节抄的，袁飞翔当场打开设计说明对了一遍，一致                         | 无   |
| 10   | 杨子航 | JwtServiceTest 篡改用例只改 1 个字符，改到 base64 填充位会不会仍然合法               | 改的是第 6 个字符，在载荷中段，不是填充。签名校验对任何一位都敏感。不改             | 无   |
| 11   | 孟甲   | xlsx 用例真的生成了一个 xlsx 文件                                                    | 用 POI 在内存里造，不落盘，30 毫秒。就是要测 DataFormatter 不把数字学号读成科学计数 | 无   |
| 12   | 张家森 | AccountServiceTest 有 27 条，会不会太碎                                              | 每条一个边界，碎是对的。合并了失败时看不出哪个边界错                                | 无   |

## 第三场：袁飞翔讲集成测试

| 序号 | 提出人 | 问题                                                     | 结论                                                                                                            | 谁改 |
| ---- | ------ | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---- |
| 13   | 杨子航 | 集成测试为什么不用 @Transactional 回滚，非要让库越跑越大 | 要测的就是跨事务：改密后再登录、锁定后再登录，回滚了就测不到。学号带时间戳互不干扰                              | 无   |
| 14   | 周浩然 | 主线用例改了一处前置才过，会不会是为了让它过而改         | 改前置是因为按走查第 4 条的设计，文本没变时提交不重复存快照，原用例假设错。改后测的是强制存那条路径，比原来更准 | 无   |
| 15   | 丁浩然 | 触发器用例直接写 SQL，绕过了服务层                       | 就是要绕过。测的是"哪怕有人拿到数据库权限，删快照也删不掉"（AC-WRK-02-4）                                       | 无   |
| 16   | 孟甲   | 没设环境变量时 5 条跳过，CI 上会不会永远跳过             | 会。实验 11 部署时在 Compose 里起 MySQL 跑一次。先在 README 写清                                                | 已写 |

## 第四场：丁浩然讲前端测试与 EditorView 修改

| 序号 | 提出人 | 问题                                                        | 结论                                                                                    | 谁改 |
| ---- | ------ | ----------------------------------------------------------- | --------------------------------------------------------------------------------------- | ---- |
| 17   | 袁飞翔 | 在途用例里 inFlight.resolve 之后 flushPromises 一次够不够   | save() 里 await 之后只有同步赋值，一次够。多 flush 一次也无害。不改                     | 无   |
| 18   | 周浩然 | 修复后 saving 期间的输入回到 dirty，谁负责下一次保存        | onInput 已经 scheduleSave 了 5 秒定时器，save() 不再排。走了一遍状态表                  | 无   |
| 19   | 杨子航 | 断网用例推进 10 秒是 setInterval，成功后 clearInterval 了吗 | 修复时把 clearInterval 提到了判断之前，无论文本变没变都清。用例里 retry 后只 PUT 了两次 | 无   |
| 20   | 孟甲   | 假定时器为什么不 fake nextTick                              | fake 了 flushPromises 就卡死，这是初稿没踩到但差点踩的坑，记在修正记录                  | 已记 |
| 21   | 张家森 | 禁用词扫描跳过 *.test.ts 会不会成为漏洞                     | 测试文件不是用户看到的文案；产品文件一个都没跳。不改                                    | 无   |

## 第五场：三个修 bug 的人各讲一条

| 序号 | 讲的人 | 改了什么               | 哪条用例证明改对了                                    | 大家的问题                                                                                                                                           |
| ---- | ------ | ---------------------- | ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 22   | 杨子航 | D-1 改密补 users.save  | 集成 changedPasswordIsWhatTheNextLoginChecks          | 周浩然：其他改脱管实体的地方呢？查了一遍，login 里的 user 是事务内查的，托管；其余 Service 都是事务内 require 出来的。只有 changePassword 拿的是参数 |
| 23   | 周浩然 | D-7 reconstruct 加上界 | reconstructRefusesACircularDeltaChainInsteadOfHanging | 丁浩然：上界为什么是 keyframeEvery 不是 keyframeEvery - 1？正常最多 9 步，取 10 留一步余量，且抛的是 IllegalStateException 不是 NotFound             |
| 24   | 丁浩然 | D-10 记下 sent 再比    | 前端 "保存请求在途时的新输入不能被成功回调抹掉"       | 袁飞翔：比的是字符串相等，5 万字符比一次多久？纳秒级，且只在成功回调里比一次                                                                         |

## 统计

| 项         | 数量        |
| ---------- | ----------- |
| 走查问题   | 24          |
| 改测试代码 | 2（加注释） |
| 改产品代码 | 0           |
| 确认不改   | 20          |
| 记录到文档 | 2           |

产品代码本周的 12 处修改全部在审查阶段完成，走查没再发现新的产品问题。
